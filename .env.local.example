# Copy this file to `.env.local` and fill in your values (see CLAUDE.md §6.2 / §6.5).
# These are PUBLIC client keys only — never put a service-role or Gemini key here.

# From Supabase → Project Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
